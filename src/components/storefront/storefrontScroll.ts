/** Web scroll classes for storefront (wired in public/index.html). */

/** One vertical storefront scroller — chrome stays put, body scrolls. */
export const STOREFRONT_SCROLL_CLASS = 'gj-storefront-scroll';

/** Chrome / chip horizontal strips — pan-x without trapping vertical scroll. */
export const STOREFRONT_H_SCROLL_CLASS = 'gj-storefront-h-scroll';
