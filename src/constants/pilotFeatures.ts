/** Pilot scope flags — flip when re-enabling deferred features post-pilot. */

/** Build 8 — Hanukkah 2026 pilot ships parent shell only; kid profiles deferred to tablet. */
export const PILOT_PARENT_ONLY = true;

/** In-app Hanukkah Guide is print-in-box; hide app routes when true. */
export const PILOT_HIDE_IN_APP_GUIDE = true;
